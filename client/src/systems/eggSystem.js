import React, {useEffect} from 'react'


const eggHandler = (entities, args) => {
        // const eggs = entities.every(e => e.egg);
        // for (let i = 0; i < eggs.length; i++) {
        // }
        return entities
    }

export default eggHandler